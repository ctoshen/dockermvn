version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test
      - gradle test --debug
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - target/my-app-1.0-SNAPSHOT.jar
